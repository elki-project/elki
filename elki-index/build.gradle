description = 'ELKI - Index API'
dependencies {
  compile project(':elki-core-distance') // For Euclidean distance optimizations
  compile project(':elki-persistent')
  testCompile group: 'junit', name: 'junit', version:'[4.8,)'
}
