description = 'ELKI - Persistence Layer'
dependencies {
  // We require integer ids here, for now.
  compile project(':elki-core-dbids-int')
  testCompile group: 'junit', name: 'junit', version:'[4.8,)'
}
